# BilsenBol — персональный маршрут поступления

> **Демо:** ⚠ ЗАПОЛНИТЬ — ссылка на развёрнутый сайт
> **Кейс:** LOCUS Startup Hackathon 2026, кейс 02 «Персональный маршрут поступления» (`LOCUSCASE2`)

BilsenBol превращает профиль школьника в понятный маршрут поступления: куда
подаваться, почему именно туда и что сделать на этой неделе.

---

## 1. Задача

Поступление — это десятки решений: направление, страна, вуз, экзамены,
документы, сроки. Информация разрознена, а рекомендации слишком общие, поэтому
абитуриент не понимает, что подходит именно ему и какое действие выполнить
сейчас.

**Аудитория:** школьники 9–11 классов и выпускники из стран СНГ, которые
поступают на бакалавриат, в том числе за рубеж.

## 2. Решение

Один связный путь из семи этапов кейса:

| Этап кейса | Что видит пользователь в BilsenBol |
|---|---|
| 1. Вход | Лендинг с ценностью сервиса, кнопка «Начать» и три пресета «в один клик» |
| 2. Профиль | Анкета из 6 вопросов, по одному на экран: класс, средний балл, английский, семейный бюджет, интересы, регионы |
| 3. Диагностика | Экран «Разбор»: резюме профиля, образовательная цель, статус готовности, сильная сторона, главное узкое место, запас времени |
| 4. Рекомендации | Программы, прошедшие обязательные условия. У каждой — оценка совпадения, причины, компромисс, что может помешать и как усилить заявку |
| 5. Сравнение | Две выбранные программы лицом к лицу по 10 критериям, с отметкой лучшего значения в каждой строке |
| 6. Roadmap | План по сезонам (осень, зима, весна): экзамены, документы, письма, заявки, гранты, активности, виза. Когда все шаги выполнены, открывается страница «ИИ-план»: Gemini даёт стратегию, разбор каждого шага и предложения сверх плана |
| 7. Следующее действие | Карточка «Следующий шаг» с кнопкой «Выполнено», чекбоксы шагов и шкала готовности в процентах |

Как это закрывает обязательные требования кейса к UX/UI:

- **Где я и что дальше** — степпер из четырёх шагов виден на любой ширине экрана, в анкете есть счётчик «N / 6», на плане выделен следующий шаг.
- **Объяснения человеческим языком** — у каждой рекомендации есть причины, компромисс, возможная помеха и совет, а также разбивка оценки по факторам.
- **Единая графическая система** — все цвета заданы токенами в одном файле; иконки Lucide; общий компонент кнопок; единые карточки и состояния.
- **Мобильная версия** — проверена на ширине 360 и 390 px, без горизонтальной прокрутки.
- **Реакция на изменение профиля** — панель «Ваши условия» на экранах результата меняет бюджет, английский, интересы, регионы и балл. После каждой правки плашка «Что изменилось» показывает, что именно сдвинулось: число программ, статус, лидер подборки, позиции в рейтинге, шаги плана. Если правка ни на что не повлияла, она так и говорит.
- **Источники и демо-пометки** — каталог программ демонстрационный и помечен «Демо-данные» на каждой карточке, в сравнении и в шапке; советы плана помечены как общие ориентиры.

## 3. Стек

| Слой | Технология |
|---|---|
| Фреймворк | Next.js 16.3.5 (App Router, Turbopack) |
| UI | React 19.3, CSS Modules, собственная дизайн-система на CSS-токенах |
| Язык | TypeScript 5.9, режим `strict` |
| Иконки | lucide-react 1.47 |
| Качество кода | ESLint 9 (`eslint-config-next`), `tsc --noEmit` |
| ИИ | Google Gemini API (`gemini-3.5-flash-lite`, запасная `gemini-3.1-flash-lite`; REST `generateContent`), ответ проверяется через Zod 4 |
| Хранение | `localStorage` в браузере (база данных не используется) |
| Менеджер пакетов | npm, Node.js ≥ 20.9 |

## 4. Архитектура

Модульный монолит: бизнес-правила отделены от интерфейса.

```text
src/
  app/                    маршруты: / (лендинг), /journey (путь),
                          /journey/ai (страница ИИ-плана),
                          /api/roadmap (серверный эндпоинт ИИ)
  domain/                 чистые функции без React:
    profile.ts              профиль, варианты ответов, пресеты, валидация
    matching.ts             жёсткие ограничения, скоринг, объяснения
    diagnosis.ts            статус, резюме, сильная сторона, узкое место
    roadmap.ts              план по сезонам и расчёт прогресса
    comparison.ts           строки сравнения двух программ
  data/programs.ts        демонстрационный каталог (вымышленные вузы)
  features/               экраны пути: profile, diagnosis, recommendations,
                          comparison, roadmap, progress, journey
  components/             общие компоненты: кнопки, фон, появление при прокрутке
  lib/                    утилиты: безопасный localStorage, склонения, анимация чисел
  server/ai/              только сервер: промпт и адаптер Gemini
```

**Поток данных.** Единственный источник состояния — профиль. Разбор, подборка
и план каждый раз заново вычисляются из профиля, а не правятся по месту,
поэтому любая правка сразу отражается на всех экранах.

**Логика рекомендаций** — прозрачные правила, без «чёрного ящика».

1. **Жёсткие ограничения** отсекают программу целиком: регион вне выбранных; стоимость выше бюджета (при бюджете «$0» нужен полный грант); минимальный балл выше вашего; обязательный языковой сертификат без подготовительного года, если сертификата нет.
2. **Мягкие факторы** складываются в оценку совпадения от 0 до 100:

   | Фактор | Баллы |
   |---|---|
   | Проходит обязательные требования | 6 |
   | Совпадает с выбранным направлением | 34 |
   | Полностью покрывается грантом / запас бюджета | 6–18 |
   | Язык: сертификат есть / не требуется / есть Foundation | 14 / 12 / 9 |
   | Средний балл выше минимального | 0–14 |
   | Выбранный регион | 8 |
   | Поступление сразу после 11 класса | 6 |

   Оценка показывает, **насколько программа совпадает с профилем**. Это не вероятность поступления: сервис не обещает шансов и не гарантирует поступление.

3. **Объяснение** каждой рекомендации собирается из тех же факторов: почему подходит, какой компромисс, что может помешать, как усилить заявку.

**ИИ-план.** Скелет плана и прогресс всегда строятся правилами: они
детерминированы, работают без сети и не зависят от квоты. ИИ вынесен на
отдельную страницу `/journey/ai`. Кнопка «Открыть ИИ-план» появляется на шаге
«План», когда выполнены все шаги (прогресс 100%). Если открыть страницу
напрямую раньше, она покажет, сколько шагов осталось, и модель вызываться не
будет. Без сохранённого маршрута страница предложит его построить.

1. Сервер заново строит план из проверенного профиля — данным клиента о плане он не доверяет — и отправляет модели анкету **словами, без чисел** («хороший балл на уровне грантовых конкурсов», а не «4.6»).
2. Модель возвращает JSON по схеме: стратегию, совет к каждому шагу и до трёх дополнительных шагов. Допустимые id шагов переданы прямо в схему.
3. Ответ проверяется в коде: шаги с чужими id, тексты длиннее лимита, ссылки и **любые цифры** отбрасываются поштучно. Так выдуманные даты, цены, баллы и шансы, запрещённые кейсом, не попадают на экран, даже если модель их напишет.
4. Если ключа нет, обе модели не ответили вовремя, закончилась квота или ответ не прошёл проверку, страница честно пишет «ИИ сейчас недоступен — попробуйте открыть страницу чуть позже». Сам маршрут и прогресс от ИИ не зависят.
5. **Выбор модели — по замерам на бесплатном тарифе.** Новейшая `gemini-3.8-flash` не ответила за 45 секунд даже на запрос из одного слова, `gemini-3.5-flash` вернула 503 «high demand», а `gemini-2.5-flash` отклонила схему ответа как слишком сложную. `gemini-3.5-flash-lite` отвечает на полный запрос за 3–4 секунды. Если основная модель перегружена или не укладывается в 12 секунд, сервер сразу пробует запасную.
6. Один профиль даёт один запрос: результат кэшируется на сервере и в браузере, быстрые правки подряд склеиваются в один запрос, модель вызывается только на странице ИИ-плана. Сервер пропускает не больше 20 обращений к Gemini в минуту — это защищает бесплатную квоту.

**Сохранение.** Профиль, текущий шаг и отмеченные шаги плана пишутся в
`localStorage` (ключ `bilsenbol.journey`, версия схемы 1). При чтении данные
проверяются поле за полем; повреждённые данные или данные другой версии
отбрасываются целиком. Если хранилище недоступно, путь работает в памяти.
Кнопка «Начать заново» стирает прогресс после подтверждения.

## 5. Запуск

```bash
npm install
npm run dev
```

Откройте <http://localhost:3000>.

**ИИ-план (необязательно).** Скопируйте `.env.example` в `.env.local` и
укажите `GEMINI_API_KEY` — ключ из Google AI Studio. Без ключа всё работает,
только без советов ИИ. На Vercel эту переменную нужно задать в настройках
проекта.

Чтобы открыть dev-сервер с телефона в той же сети, используйте адрес из вывода
`next dev` (например, `http://192.168.x.y:3000`). Адреса `192.168.*.*`
разрешены в `next.config.ts`.

Сборка и проверки:

```bash
npm run typecheck
npm run lint
npm run build
npm start
```

Регистрация и вход не нужны, тестовый логин не требуется.

## 6. Тестовый сценарий

Повторяет проверочный сценарий жюри из кейса.

1. **Полный путь.** На главной нажмите «Начать», ответьте на 6 вопросов и нажмите «Построить мой маршрут».
2. **Разбор.** Проверьте резюме профиля, цель, статус готовности и три вывода.
3. **Рекомендации.** Нажмите «Дальше: Подбор». У программы раскройте «Подробнее»: причины, компромисс, как усилить заявку, разбивка оценки по факторам.
4. **Сравнение.** Нажмите «Сравнить» на двух карточках, затем «Сравнить» на нижней панели.
5. **Реакция на изменения.** Нажмите «Ваши условия → Изменить» и поменяйте бюджет, интерес, регион или экзамен. Плашка «Что изменилось» покажет сдвиги. Например, для IT-пресета бюджет «До $3 000» даёт «Программ: 6 → 5».
6. **Следующий шаг и прогресс.** На шаге «План» нажмите «Выполнено»: шкала готовности вырастет. Обновите страницу — прогресс сохранится.
7. **Быстрый старт.** На главной выберите пресет: «Отличник, нужен грант», «IT, средний бюджет» или «Без IELTS, нужен Foundation».
8. **ИИ-план** (если задан ключ). На шаге «План» нажимайте «Выполнено», пока все шаги не будут закрыты. Появится кнопка «Открыть ИИ-план»: она ведёт на страницу со стратегией, разбором по шагам и дополнительными предложениями. Если открыть `/journey/ai` раньше, страница покажет, сколько шагов осталось.
9. **Пустое состояние.** В IT-пресете откройте «Ваши условия», выберите бюджет «$0 — только грант» и оставьте из регионов только «США» (сначала отметьте США, затем снимите остальные). Появится «Ничего не подошло» с причинами отсева и кнопкой, которая смягчает условие, отсеявшее больше всего программ.
10. **Мобильная версия.** Пройдите путь на ширине 360–390 px.

## 7. Команда

⚠ ЗАПОЛНИТЬ

| Участник | Роль |
|---|---|
| … | … |

## 8. Источники

- **Каталог программ** (`src/data/programs.ts`) — демонстрационные данные: 14 вымышленных программ. Реальным вузам не приписано ни одного требования, дедлайна или цены. На всех экранах, где показаны данные каталога, стоит пометка «Демо-данные».
- **Советы в плане** — общие рекомендации по подготовке, а не требования конкретного вуза; об этом говорит пометка на экране плана.
- **Фотографии** — [Unsplash](https://unsplash.com), лицензия [Unsplash License](https://unsplash.com/license). Хранятся в `public/images/` и показывают только сцены учёбы: ни одного узнаваемого реального кампуса, потому что вузы в каталоге вымышленные.

  | Файл | Источник |
  | --- | --- |
  | `hero-students.jpg` | https://images.unsplash.com/photo-1522202176988-66273c2fd55f |
  | `step-profile.jpg` | https://images.unsplash.com/photo-1434030216411-0b793f4b4173 |
  | `step-match.jpg` | https://images.unsplash.com/photo-1427504494785-3a9ca7044f45 |
  | `step-plan.jpg` | https://images.unsplash.com/photo-1531482615713-2afd69097998 |

## 9. AI и API

- **Подбор, разбор и скелет плана** — прозрачные правила (rule-based логика), как разрешает кейс. Жёсткие ограничения должны быть детерминированными и проверяемыми, поэтому модель в них не участвует.
- **ИИ-персонализация плана** — Google Gemini API, модель `gemini-3.5-flash-lite` с запасной `gemini-3.1-flash-lite`, эндпоинт `generateContent` — без хранимой истории диалога, в отличие от Interactions API, где хранение включено по умолчанию. Модель пишет стратегию, советы к шагам и дополнительные предложения; как ограничены её ответы — см. раздел 4, «ИИ-слой плана».
- **Какие данные уходят в модель:** только анонимные поля анкеты, описанные словами (класс, уровень балла, английский, уровень бюджета, интересы, регионы), и названия демо-программ. Имён, контактов и других личных данных сервис не собирает. Условия использования данных зависят от тарифа Gemini API.
- **Ключ API** хранится только в переменных окружения сервера и в браузер не передаётся.
- **При разработке** команда использовала ИИ-ассистентов для кода. Правила их работы описаны в `AGENTS.md`, журнал задач ведётся в `AGENTS_CHANGELOG.md`.

## 10. Готовые компоненты

- **Lucide** (`lucide-react`, лицензия ISC) — иконки.
- **Next.js** — встроенные `next/image` (оптимизация фото) и `next/link`.

UI-кит не использовался: кнопки, карточки, анкета, степпер, сравнение, таймлайн
и фон написаны в проекте.

## 11. Ограничения

- Каталог — демо-данные вымышленных вузов, поэтому рекомендации демонстрируют работу правил, а не реальные маршруты.
- Из экзаменов учитывается только английский: IELTS, TOEFL, Duolingo или школьный уровень. Национальные экзамены (ЕНТ, ЕГЭ, SAT) не моделируются.
- Регионы, а не отдельные страны.
- Прогресс хранится в браузере: нет аккаунтов и синхронизации между устройствами.
- Сравниваются ровно две программы.
- Реальных дедлайнов нет, только сезоны. Окна подачи в каталоге — демо-данные.
- ИИ-советы проверяются на форму, длину, ссылки и цифры, но не на смысл: модель может дать неточный или слишком общий совет. Поэтому они подписаны «это советы, а не требования вузов».
- На бесплатном тарифе Gemini квота ограничена; когда она кончается, показывается план без ИИ.
- Автотестов в репозитории пока нет (см. раздел 12).

## 12. Техническая справка

| Пункт | Значение |
|---|---|
| Модели | Google `gemini-3.5-flash-lite`, запасная `gemini-3.1-flash-lite` (список задаётся переменной `GEMINI_MODEL` через запятую) |
| Внешние API и сервисы | Gemini API (`generateContent`); фотографии хранятся локально |
| Библиотеки | next, react, react-dom, lucide-react, zod; для разработки — typescript, eslint, eslint-config-next |
| Данные | `src/data/programs.ts` — демо-каталог; профиль и прогресс хранятся в браузере (`localStorage`); в Gemini уходят только анонимные поля анкеты словами |

**Методы проверки.**

- `npm run typecheck`, `npm run lint`, `npm run build` — завершаются без ошибок.
- Доменная логика проверялась отдельными прогонами: у каждого пресета ≥ 3 программ; жёсткие ограничения никогда не нарушаются; невозможный профиль даёт 0 программ с полным разбором причин; смена бюджета, языка, интереса и региона меняет подборку, разбор или план; расчёт прогресса.
- Интерфейс проверялся в headless Chrome с эмуляцией касаний на 360 и 390 px: нажатие каждого основного элемента, сохранение после перезагрузки, повреждённое и недоступное хранилище, плашка «Что изменилось», отсутствие горизонтальной прокрутки.
- ИИ-слой, живые запросы к Gemini: для всех трёх пресетов ответ пришёл за 3,3–4,0 с через `/api/roadmap`; фильтр пропустил все стратегии, 12/12, 12/12 и 11/11 советов к шагам и все дополнительные шаги — модель не вставила ни одной цифры; повторный запрос того же профиля отдаётся из кэша сервера за миллисекунды; на странице ИИ-плана все 12 шагов получили совет, в текстах ИИ ноль цифр, ошибок в консоли нет.
- ИИ-слой, фильтр ответа проверен на подложенных датах, баллах, ценах, ссылках, чужих id, лишних и сломанных полях; в данных для модели нет ни одной цифры; адаптер Gemini проверен с подменённым `fetch` на успех, таймаут, квоту (429), блокировку, обрыв по длине и мысли модели в ответе; в браузере через перехват запроса: шаг «План» не вызывает модель; до 100% кнопки нет, а прямой заход показывает замок без запроса; без маршрута — пустое состояние; после всех «Выполнено» кнопка ведёт на страницу; загрузка, стратегия, разбор по шагам; подложенная дата вычищена; кэш после перезагрузки; честное «ИИ сейчас недоступен».
- Эти прогоны выполнялись во время разработки и **не входят в репозиторий** — перенести их в Vitest и Playwright запланировано следующим шагом.
